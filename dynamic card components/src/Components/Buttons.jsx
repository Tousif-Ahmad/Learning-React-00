import React from 'react'

const Buttons = ({text}) => {
  return (
    <button className='px-5 py-2 bg-blue-500 rounded-[25px] cursor-pointer text-white'>{text}</button>
  )
}

export default Buttons